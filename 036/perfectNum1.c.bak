#include <stdio.h>

int main(int argc, char** argv) {
    int i,num;
    int sum;
    for(num=1; num<=1000; num++)
    {
        sum=0;
		i=1;
		//for(i=1; i<=num; i=i)
        //{
            while(i<=num)
            {
                if (num%i==0 && i!=num)
                {
                    sum=sum+i;
                    //break;
                }
                //else if (num%i==0 && i==num)
                //{
                //    break;
                //}
                //else i=i+1;
				i++
            }
        //}
        //num=num/i;
    //}
		if(num==sum)
		{
			printf("%d ",num);
		}
	}	
	printf("\n");
    return 0;
}